{% extends 'base.html.twig' %}

{% block title %}Accommodation{% endblock %}

{% block body %}
    <h1>Accommodation</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ accommodation.id }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ accommodation.type }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ accommodation.description }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td>{{ accommodation.image }}</td>
        </tr>
        <tr>
            <th>Author Id</th>
            <td>{{ accommodation.authorId }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('accommodation_index') }}">back to list</a>

    <a href="{{ path('accommodation_edit', {'id': accommodation.id}) }}">edit</a>

    {{ include('accommodations/_delete_form.html.twig') }}
{% endblock %}